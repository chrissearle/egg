[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/playfield.gd" id="1_playfield"]

; Position is set from Playfield.PLAY_AREA_TOP in _ready(), so the HUD
; boundary has a single definition in code rather than drifting from the
; scene file.
[node name="Playfield" type="Node2D"]
script = ExtResource("1_playfield")
